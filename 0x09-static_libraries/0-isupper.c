#include "main.h"
/**
 * _isupper- creating upper case character
 * @c: check
 *
 * return: zero or number 1
 */

int _isupper(int c)

{
        	if (c >= 'A' && c <= 'Z')
                    	return (1);
        	else
        	return (0);
}

